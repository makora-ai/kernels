# AMD MI300X Kernels

GPU kernels targeting **AMD MI300X** accelerators.

This directory contains kernel implementations generated automatically using **MakoraGenerate** and validated on MI300X hardware.

Kernels are implemented using the appropriate AMD backend (HIP, Triton-ROCm, or other ROCm-compatible tooling).
